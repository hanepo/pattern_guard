name: pattern_analyzer
description: Smart Android Graphical Password Strength Analyzer

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.68.0
  shared_preferences: ^2.2.2
  pdf: ^3.10.8
  path_provider: ^2.1.2
  share_plus: ^7.2.2
  audioplayers: ^5.2.1
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/